/// AarogyaMP — Register Screen stub (Person C, M1)
/// Route: /register — includes role selection (patient / doctor)
import 'package:flutter/material.dart';

class RegisterScreen extends StatelessWidget {
  const RegisterScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Register Screen — M1')));
  }
}
