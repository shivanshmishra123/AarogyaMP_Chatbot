/// AarogyaMP — Voice message widget stub (Person C, M1)
/// Cap length at 2 minutes, show waveform/duration before sending (Reference §14).
import 'package:flutter/material.dart';

class VoiceMessageWidget extends StatelessWidget {
  const VoiceMessageWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 60);
  }
}
