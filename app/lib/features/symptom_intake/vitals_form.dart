// AarogyaMP — Vitals entry form stub (Person C, M1)
// Route: /vitals — inline validation for obviously-invalid values before submit
import 'package:flutter/material.dart';

class VitalsFormScreen extends StatelessWidget {
  const VitalsFormScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Vitals Form — M1')));
  }
}
