// AarogyaMP — Symptom text input screen stub (Person C, M1)
// Route: /symptoms/text
import 'package:flutter/material.dart';

class SymptomTextInputScreen extends StatelessWidget {
  const SymptomTextInputScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Symptom Text Input — M1')));
  }
}
